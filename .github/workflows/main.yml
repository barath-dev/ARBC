name: Deploy ARBC-Backend

on:
  push:
    branches:
      - main
      - dev

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: "${{ secrets.DOCKER_USERNAME }}"
          password: "${{ secrets.DOCKER_PASSWORD }}"

      - name: Build and Push Multi-Arch Docker Image
        run: |
          cd source_code/server
          docker buildx build \
          --platform linux/amd64,linux/arm64 \
          -t barathwaj911/arbc-backend:latest \
          --build-arg NODE_ENV=production \
          --no-cache \
          --push .

  deploy-main:
    needs: build
    if: github.ref == 'refs/heads/main'
    runs-on: main
    steps:
      - name: Debug Environment
        run: |
          echo "Path: $PATH"
          echo "Docker location: $(which docker || echo 'not found')"
          ls -l /usr/bin/docker || echo "/usr/bin/docker not found"

      - name: Stop and Remove Existing Container (if exists)
        run: |
          /usr/bin/docker stop arbc-backend-container || true
          /usr/bin/docker rm arbc-backend-container || true

      - name: Remove old images
        run: |
          /usr/bin/docker image prune -af --filter "label=com.docker.compose.project=arbc"
          /usr/bin/docker rmi barathwaj911/arbc-backend:latest || true

      - name: Pull Image from Docker Hub
        run: /usr/bin/docker pull barathwaj911/arbc-backend:latest

      - name: Run Docker Container
        run: |
          /usr/bin/docker run -d -p 3000:3000 --name arbc-backend-container \
            -e DATABASE_URL='${{ secrets.DATABASE_URL }}' \
            -e JWT_SECRET='${{ secrets.JWT_SECRET }}' \
            -e JWT_EXPIRES_IN='${{ secrets.JWT_EXPIRES_IN }}' \
            -e GITHUB_TOKEN='${{ secrets.GITHUB_TOKEN }}' \
            -e PORT='3000' \
            -e NODE_ENV='production' \
              barathwaj911/arbc-backend:latest

      - name: Clean up old images
        run: /usr/bin/docker image prune -af

  deploy-dev:
    needs: build
    if: github.ref == 'refs/heads/dev'
    runs-on: dev
    steps:
      - name: Debug Environment
        run: |
          echo "Path: $PATH"
          echo "Docker location: $(which docker || echo 'not found')"
          ls -l /usr/bin/docker || echo "/usr/bin/docker not found"

      - name: Stop and Remove Existing Container (if exists)
        run: |
          /usr/bin/docker stop arbc-backend-container || true
          /usr/bin/docker rm arbc-backend-container || true

      - name: Remove old images
        run: |
          /usr/bin/docker image prune -af --filter "label=com.docker.compose.project=arbc"
          /usr/bin/docker rmi barathwaj911/arbc-backend:latest || true

      - name: Pull Image from Docker Hub
        run: /usr/bin/docker pull barathwaj911/arbc-backend:latest

      - name: Run Docker Container
        run: |
          /usr/bin/docker run -d -p 3000:3000 --name arbc-backend-container \
            -e DATABASE_URL='${{ secrets.DATABASE_URL }}' \
            -e JWT_SECRET='${{ secrets.JWT_SECRET }}' \
            -e JWT_EXPIRES_IN='${{ secrets.JWT_EXPIRES_IN }}' \
            -e GITHUB_TOKEN='${{ secrets.GITHUB_TOKEN }}' \
            -e PORT='3000' \
            -e NODE_ENV='development' \
              barathwaj911/arbc-backend:latest

      - name: Clean up old images
        run: /usr/bin/docker image prune -af
